{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": { "type": "string", "minLength": 1 },
      "evidence_type": {
        "type": "string",
        "enum": [
          "Insoluble A&beta;40",
          "Insoluble A&beta;42",
          "NfL",
          "Soluble A&beta;40",
          "Soluble A&beta;42"
        ]
      },
      "tissue": {
        "type": "string",
        "enum": ["Cerebral Cortex", "Hippocampus", "Plasma"]
      },
      "age": {
        "type": "string",
        "enum": ["4 months", "12 months", "18 months"]
      },
      "units": {
        "type": "string",
        "enum": ["pg/mg", "pg/ml"]
      },
      "y_axis_max": { "type": "number" },
      "data": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "individual_id": { "type": "string", "minLength": 1 },
            "value": { "type": "number" },
            "sex": { "type": "string", "enum": ["Female", "Male"] },
            "genotype": { "type": "string", "minLength": 1 }
          },
          "required": ["individual_id", "value", "sex", "genotype"],
          "additionalProperties": false
        }
      }
    },
    "required": ["name", "evidence_type", "tissue", "age", "units", "y_axis_max", "data"],
    "additionalProperties": false
  }
}
